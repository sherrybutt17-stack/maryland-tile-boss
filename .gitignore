node_modules/

# Intermediate crawl artefacts — 33MB of raw HTML from the old site.
# Regenerate with: python3 scripts/crawl.py
research/raw/

# Production build artefacts. Copied from assets/optimized and assets/fonts by
# scripts/build.mjs, so committing them would store the same images twice over.
# Regenerate both sites in a second with: npm run site
# Leading slashes matter — these must NOT match the docs/ preview, which is committed
# in full so GitHub Pages can serve it.
/version-a/assets/img/
/version-a/assets/fonts/
/version-b/assets/img/
/version-b/assets/fonts/

.DS_Store
*.log
